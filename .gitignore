__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/
.ruff_cache/
.pytest_cache/
# What the solve job writes and the dashboard reads; regenerate with `showcase-solve`.
runs/
# The what-if grid, the same kind of archive; regenerate with `showcase-grid`.
grid/
# The site: dependencies, the data-loader cache, and the build Pages serves.
site/node_modules/
site/dist/
site/src/.observablehq/cache/
# The executed notebook the site embeds; `marimo export html` writes it.
site/src/session.html
# marimo's per-notebook session cache.
notebooks/__marimo__/
